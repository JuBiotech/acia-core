name: Acia

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      projectId: {type: str}
      user: {type: str}
      serverUrl: {type: str}
      modelUrl: {type: str, default: "http://ibt056/pt/predictions/cellcmaskrcnn/"}

    command: "python setup.py install && python examples/onlineProjectPrediction.py --project-id {projectId} --user {user} --server-url {serverUrl} --model-url {modelUrl}"
